# kk-033 Kyusu teapot

reference: `/Volumes/zug1/kyoto-kat-kit/v1/images/kk-033.png` · manifest target 0.19 × 0.14 × 0.12 m
status: production source complete; one bounded critic correction applied; unreviewed post-correction

The model uses a low rounded vessel, domed lid with a seated knob and rim, a short raised spout with a
visible dark opening and base collar, and a thick cylindrical side handle rather than a loop. The +X
handle / −X spout arrangement follows the reference; the handle can be mirrored through configuration.
Shared ceramic, moss, brass, and deep-glaze slots separate the major material reads without adding textures
or branding.

Runtime parts are `body`, `lid`, `spout`, and `handle`. `lid` and `handleSide` rebuild in place while keeping
the root and semantic groups stable. Hidden underside and internal tea strainer details are plausible
reconstructions omitted at the storytelling budget.

Dimensions: 0.202 × 0.114 × 0.110 m; inventory: 1952 triangles, within the 2000-storytelling cap but
within the 15% warning band. The four used material slots are intentional for ceramic, moss ceramic, brass
trim, and the dark spout opening; inventory reports the kit guideline warning for material count. The
handle end is left as the low-cost tube termination used to preserve the triangle budget. The coordinator
owns topology sidecar generation and rendering.
