# kk-032 Tea caddy

reference: `/Volumes/zug1/kyoto-kat-kit/v1/images/kk-032.png` · manifest target 0.12 × 0.12 × 0.16 m
status: production source complete; one bounded critic correction applied; unreviewed post-correction

The model uses a rounded square tin body, a stepped raised lid with fuller rounded edges, narrow top/base
trim bands, and a thin 0.080 m square blank front paper label set 0.00025 m proud. The moss glaze material is a stylized patinated-metal proxy
from the shared palette; no text or branding is authored. Hidden underside construction is a simple closed
body with a plausible tin base.

Runtime parts are `body`, `lid`, and `label`. `lid` and `label` are configurable without replacing the
root or semantic groups. No texture or sidecar is added; the coordinator owns topology compilation and
rendering.

Dimensions: 0.118 × 0.118 × 0.153 m including raised cap; 0.112 m body width/depth. Inventory: 1288 triangles,
2 materials, no budget breach.

Open caveat: the reference has distressed paint and floral decoration, while this production source keeps
the label blank and represents the surface as broad shared-material areas within the storytelling budget.
